{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to MkDocs","text":"<p>For full documentation visit mkdocs.org.</p>"},{"location":"#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"hw1/Task1/","title":"Task 1: Drawing Single-Color Triangles","text":""},{"location":"hw1/Task1/#triangle-rasterization-algorithm","title":"Triangle Rasterization Algorithm","text":"<p>To rasterize triangles, I implemented a bounding box approach. First, I determine the bounding box of the triangle by finding the minimum and maximum x and y coordinates of the three vertices, clamping them to the screen resolutions. I then iterate through every pixel coordinate \\((x, y)\\) within this bounding box.</p> <p>For each pixel, I calculate the center point \\((x + 0.5, y + 0.5)\\) and perform a point-in-triangle test. This is done using the three test functions derived from the cross-product of the triangle's edges. If the point lies on the correct side of all three half-planes (either all positive or all negative, depending on the winding order), the pixel is considered inside the triangle and is colored using <code>fill_pixel</code>.</p>"},{"location":"hw1/Task1/#efficiency-check","title":"Efficiency Check","text":"<p>My algorithm is no worse than one that checks each sample within the bounding box because it does exactly that: it restricts the search space to the rectangle defined by \\([min\\_x, max\\_x] \\times [min\\_y, max\\_y]\\). It never checks pixels outside this range, ensuring the complexity is proportional to the area of the bounding box rather than the entire framebuffer.</p>"},{"location":"hw1/Task1/#result-screenshot","title":"Result Screenshot","text":"<p>Figure 1: test4.svg</p> <p><code>test4.svg</code>: I have the pixel inspector open on the thin red wedge. This shows that my rasterizer correctly finds the center points of pixels even for very thin and sharp triangles.</p> <p><code>test6.svg</code>: This test contains triangles with different winding orders. My code handles this by checking if the line equation results are either all positive or all negative. All shapes are rendered correctly.</p>"},{"location":"hw1/Task1/#extra-credit-optimizations","title":"Extra Credit: Optimizations","text":"<p>I implemented two optimizations to improve rasterization speed beyond the simple bounding box check.</p> <ol> <li>Incremental Test Function \\(L\\) Updates: Instead of computing the full test function \\(L_i(x,y)=-(x-X_i)dY_i+(y-Y_i)dX_i\\) for every pixel, I calculate an initial value at the start of the loop. For each step in the inner loop (as x increases), I simply update \\(L\\) by adding \\(-dY\\). When moving to the next row (as y increases), I add \\(dX\\). This avoids performing time-consuming multiplication operations for every single pixel.</li> <li>Early Exit: Since a triangle is a convex shape, if we are iterating along a line and have already entered the triangle, the moment we step outside the triangle, we know we will not re-enter it on that same line. I added a flag <code>entered</code>; if a pixel fails the edge check and <code>entered</code> is true, I immediately <code>break</code> the inner loop.</li> </ol>"},{"location":"hw1/Task1/#performance-comparison","title":"Performance Comparison","text":"<p>I tested these changes using <code>test3.svg</code>. The Early Exit provided the biggest speed boost.</p> Version Average Time (ms) Speedup Baseline (Naive Bounding Box) 10.341 1.0x Optimization 1 (Incremental Updates) 9.427 ~1.10x Optimization 2 (Early Exit) 6.870 ~1.51x"},{"location":"hw1/Task2/","title":"Task 2: Antialiasing by Supersampling","text":""},{"location":"hw1/Task2/#supersampling-algorithm-and-data-structures","title":"Supersampling Algorithm and Data Structures","text":"<p>Supersampling is a technique used to reduce aliasing artifacts by rendering the image at a higher resolution and then downsampling it to the output resolution. This process averages the colors within a pixel, creating smoother transitions at edges.</p> <p>To implement supersampling, I modified the rasterization pipeline to support a dynamically sized <code>sample_buffer</code>.</p> <ol> <li>Data Structure: I resized <code>sample_buffer</code> from <code>width * height</code> to <code>width * height * sample_rate</code>, since each pixel in the original image is represented by <code>sample_rate</code> sub-pixels.</li> <li>Rasterization (<code>rasterize_triangle</code>):<ul> <li>Instead of testing a single point at the center of each pixel \\((x+0.5,y+0.5)\\), I divided each pixel into a \\(\\sqrt{\\text{sample\\_rate}}\\times\\sqrt{\\text{sample\\_rate}}\\) grid. I iterate through this sub-grid, checking if the center of each sub-pixel is inside the triangle. Then I store the color of the triangle in the specific index of <code>sample_buffer</code> corresponding to that sub-pixel.</li> <li>To maintain the efficiency optimizations from Task 1, I applied the incremental test function updates to work on this sub-pixel grid. The step size for the test functions \\((dX, dY)\\) is scaled by \\(1/\\sqrt{\\text{sample\\_rate}}\\).</li> <li>Also, for the \u201cearly exit\u201d from Task 1, I organized the loops to iterate over the high-resolution sample_buffer. By treating the pixel \\(y\\) coordinate and the sub-pixel \\(\\text{sub}\\_y\\) index as a combined row iterator, I can still detect when the rasterizer leaves the triangle on a specific sub-pixel row. Once stepping out of the triangle, I immediately break the inner loop to skip the remaining empty space on that row.</li> </ul> </li> <li>Resolve to Frame buffer (<code>resolve_to_framebuffer</code>): For every pixel in the final image, I iterate over its corresponding <code>sample_rate</code> sub-pixels in the <code>sample_buffer</code>. Then calculate the average color of these sub-pixels and write the result to the frame buffer. </li> <li>Updates to Other Functions:<ul> <li><code>set_sample_rate</code> and <code>set_framebuffer_target</code>: Resize the <code>sample_buffer</code> whenever the window is resized or the sample rate changes.</li> <li><code>fill_pixel</code>: Fill all sub-pixels of a pixel with the same color, ensuring points and lines remain visible.</li> </ul> </li> </ol>"},{"location":"hw1/Task2/#result-screenshot","title":"Result Screenshot","text":"<p>Figure 1: Sample Rate 1</p> <p>Figure 2: Sample Rate 4</p> <p>Figure 3: Sample Rate 16</p> <p><code>basic/test4.svg</code> at sample rates 1, 4, and 16. The pixel inspector is focused on the tip of the thin red triangle.</p> <ul> <li>Sample Rate 1: The edges are pixelated. The thin tip of the triangle is broken because many pixel centers fall outside the narrow geometry.</li> <li>Sample Rate 4: The edges are smoother. Lighter red appear where the triangle only partially covers a pixel. The tip is better connected.</li> <li>Sample Rate 16: The edges look very smooth. The tip of the triangle is fully intact and renders as a continuous line with varying opacity.</li> </ul>"},{"location":"hw1/Task2/#why-extra-memory-is-needed","title":"Why Extra Memory is Needed","text":"<p>Is it possible to achieve the same results without more memory?</p> <p>Yes, it is possible, but it comes with significant engineering tradeoffs.</p> <p>The <code>sample_buffer</code> requires <code>sample_rate</code> times more memory than the frame buffer because rasterization happens per-triangle, not per-pixel. When we draw a triangle, we update specific sub-pixels. Later, a second triangle might overlap the first one, overwriting some of those sub-pixels. To correctly calculate the final average color, we must store the independent color value for every single sub-pixel until all triangles are drawn.</p> <p>To avoid errors, we would need to perfectly resolve the depth order of all triangles before rasterization, and draw them from back to front (Painter's Algorithm). This approach trades memory for time, because sorting geometry and handling intersecting triangles are complex and time-consuming (potentially \\(O(N \\log N)\\) or worse)</p>"}]}